version: '3'

services:
    worker:
        environment:
            DEBUG: 'True'
            DB_NAME: postgres
            DB_USER: postgres
            DB_PASSWORD: password
            DB_HOST: database
            DB_PORT: 5432
            CELERY_HOST: rabbit
            REDIS_HOST: redis
            DJANGO_SETTINGS_MODULE: reddit.settings
    web:
        entrypoint: /app/start-web.sh
        environment:
            DEBUG: 'True'
            DB_NAME: postgres
            DB_USER: postgres
            DB_PASSWORD: password
            DB_HOST: database
            DB_PORT: 5432
            CELERY_HOST: rabbit
            REDIS_HOST: redis
            DJANGO_SETTINGS_MODULE: reddit.settings
